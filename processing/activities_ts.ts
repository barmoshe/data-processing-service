// processing/activities_ts.ts

// Define the activity function that uppercases the string.
export async function toUpperCaseActivity(data: string): Promise<string> {
    return data.toUpperCase();
  }
  